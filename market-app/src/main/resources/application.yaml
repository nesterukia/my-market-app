server:
  port: 8080

spring:
  application:
    name: my-market-app
  r2dbc:
    url: ${SPRING_DATASOURCE_URL}
    username: ${SPRING_DATASOURCE_USERNAME}
    password: ${SPRING_DATASOURCE_PASSWORD}
  data:
    redis:
      host: ${SPRING_DATA_REDIS_HOST}
      port: ${SPRING_DATA_REDIS_PORT}
      password: ${SPRING_DATA_REDIS_PASSWORD}
    cache:
      redis:
        time-to-live: PT5M
  thymeleaf:
    enabled: true
    cache: false
#  security:
#    oauth2:
#      client:
#        registration:
#          keycloak-service:
#            provider: keycloak-private
#            client-id: market-service
#            client-secret: DYhdsEM7GitzbCwpb9gpYiSoPufddOym
#            authorization-grant-type: client_credentials
#            scope: openid
#
#          keycloak-login:
#            provider: keycloak-public
#            client-id: market-ui
#            authorization-grant-type: authorization_code
#            redirect-uri: "{baseUrl}/login/oauth2/code/{registrationId}"
#            scope: openid,profile,email
#        provider:
#          keycloak-private:
#            issuer-uri: ${KC_INNER_REALM_URI}
#
#          keycloak-public:
#            authorization-uri: ${KC_OUTER_REALM_URI}/protocol/openid-connect/auth
#            token-uri: ${KC_INNER_REALM_URI}/protocol/openid-connect/token
#            jwk-set-uri: ${KC_INNER_REALM_URI}/protocol/openid-connect/certs
#            user-info-uri: ${KC_INNER_REALM_URI}/protocol/openid-connect/userinfo
#            user-name-attribute: preferred_username
paymentService:
  baseUrl: ${PAYMENT_SERVICE_BASE_URL}
logging:
  level:
    root: DEBUG
    org.springframework: DEBUG
    org.springframework.security: TRACE
    org.springframework.web: DEBUG
    org.springframework.web.reactive: DEBUG
    org.springframework.security.oauth2: TRACE
    org.springframework.web.reactive.function.client: TRACE
    reactor.netty: DEBUG
    reactor.netty.http.client: DEBUG
    org.springframework.boot.web.reactive.error: DEBUG
    com.github.nesterukia: DEBUG
    org.springframework.r2dbc: DEBUG
    org.springframework.security.oauth2.client: TRACE  # Детали client_credentials